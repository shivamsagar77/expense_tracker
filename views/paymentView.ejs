<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upgrade to Premium - Expense Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Cashfree SDK -->
    <script src="https://checkout.cashfree.com/v2/cashfree.js" onerror="handleSDKError()"></script>
    <style>
        :root {
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --success-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            --warning-gradient: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
            --card-shadow: 0 20px 40px rgba(0,0,0,0.1);
            --border-radius: 20px;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: var(--primary-gradient);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            position: relative;
            overflow-x: hidden;
        }

        /* Animated background elements */
        body::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="grain" width="100" height="100" patternUnits="userSpaceOnUse"><circle cx="25" cy="25" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="75" cy="75" r="1" fill="rgba(255,255,255,0.1)"/><circle cx="50" cy="10" r="0.5" fill="rgba(255,255,255,0.05)"/><circle cx="10" cy="60" r="0.5" fill="rgba(255,255,255,0.05)"/><circle cx="90" cy="40" r="0.5" fill="rgba(255,255,255,0.05)"/></pattern></defs><rect width="100" height="100" fill="url(%23grain)"/></svg>');
            animation: float 20s ease-in-out infinite;
            z-index: 0;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
        }

        .payment-container {
            position: relative;
            z-index: 1;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 2rem 0;
        }

        .payment-card {
            border: none;
            border-radius: var(--border-radius);
            box-shadow: var(--card-shadow);
            backdrop-filter: blur(10px);
            background: rgba(255, 255, 255, 0.95);
            transition: all 0.3s ease;
            overflow: hidden;
        }

        .payment-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 25px 50px rgba(0,0,0,0.15);
        }

        .payment-header {
            background: var(--secondary-gradient);
            color: white;
            border: none;
            padding: 2rem;
            text-align: center;
            position: relative;
        }

        .payment-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="dots" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23dots)"/></svg>');
            opacity: 0.3;
        }

        .payment-header h1 {
            position: relative;
            z-index: 1;
            margin-bottom: 0.5rem;
            font-weight: 700;
            font-size: 2.5rem;
        }

        .payment-header p {
            position: relative;
            z-index: 1;
            opacity: 0.9;
            margin: 0;
            font-size: 1.1rem;
        }

        .premium-badge {
            background: linear-gradient(135deg, #ffd700, #ffed4e);
            color: #333;
            font-weight: 600;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            font-size: 0.9rem;
            display: inline-block;
            margin: 1rem 0;
        }

        .features-list {
            list-style: none;
            padding: 0;
        }

        .features-list li {
            padding: 0.75rem 0;
            border-bottom: 1px solid #f0f0f0;
            display: flex;
            align-items: center;
        }

        .features-list li:last-child {
            border-bottom: none;
        }

        .features-list li i {
            color: #28a745;
            margin-right: 1rem;
            font-size: 1.2rem;
        }

        .price-display {
            text-align: center;
            padding: 2rem;
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            border-radius: 15px;
            margin: 1.5rem 0;
        }

        .price-amount {
            font-size: 3rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 0.5rem;
        }

        .price-period {
            color: #6c757d;
            font-size: 1.1rem;
        }

        .form-floating {
            margin-bottom: 1.5rem;
        }

        .form-control {
            border: 2px solid #e9ecef;
            border-radius: 15px;
            padding: 1rem 1rem 1rem 3rem;
            font-size: 1rem;
            transition: all 0.3s ease;
            background: rgba(255, 255, 255, 0.8);
        }

        .form-control:focus {
            border-color: #f093fb;
            box-shadow: 0 0 0 0.2rem rgba(240, 147, 251, 0.25);
            background: white;
        }

        .input-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: #6c757d;
            z-index: 3;
            transition: color 0.3s ease;
        }

        .form-control:focus + .input-icon {
            color: #f093fb;
        }

        .btn-payment {
            background: var(--secondary-gradient);
            border: none;
            border-radius: 15px;
            padding: 1rem 2rem;
            font-weight: 600;
            font-size: 1.2rem;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            width: 100%;
        }

        .btn-payment::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
            transition: left 0.5s;
        }

        .btn-payment:hover::before {
            left: 100%;
        }

        .btn-payment:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(240, 147, 251, 0.4);
        }

        .btn-payment:disabled {
            opacity: 0.6;
            cursor: not-allowed;
            transform: none;
        }

        .alert {
            border-radius: 15px;
            border: none;
            margin-bottom: 1.5rem;
        }

        .alert-success {
            background: linear-gradient(135deg, #4facfe, #00f2fe);
            color: white;
        }

        .alert-danger {
            background: linear-gradient(135deg, #ff6b6b, #ee5a52);
            color: white;
        }

        .alert-info {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
        }

        .loading-spinner {
            display: inline-block;
            width: 20px;
            height: 20px;
            border: 3px solid rgba(255,255,255,.3);
            border-radius: 50%;
            border-top-color: #fff;
            animation: spin 1s ease-in-out infinite;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .floating-shapes {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
        }

        .shape {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float-shapes 6s ease-in-out infinite;
        }

        .shape:nth-child(1) {
            width: 80px;
            height: 80px;
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }

        .shape:nth-child(2) {
            width: 120px;
            height: 120px;
            top: 60%;
            right: 10%;
            animation-delay: 2s;
        }

        .shape:nth-child(3) {
            width: 60px;
            height: 60px;
            bottom: 20%;
            left: 20%;
            animation-delay: 4s;
        }

        @keyframes float-shapes {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-30px) rotate(180deg); }
        }

        .back-btn {
            position: absolute;
            top: 1rem;
            left: 1rem;
            background: rgba(255, 255, 255, 0.2);
            border: none;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            color: white;
            font-size: 1.2rem;
            transition: all 0.3s ease;
        }

        .back-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: scale(1.1);
        }

        @media (max-width: 768px) {
            .payment-card {
                margin: 1rem;
            }
            
            .payment-header {
                padding: 1.5rem;
            }
            
            .payment-header h1 {
                font-size: 2rem;
            }
            
            .price-amount {
                font-size: 2.5rem;
            }
        }

        .pulse-animation {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        .fade-in {
            animation: fadeIn 0.5s ease-in;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body>
    <!-- Floating background shapes -->
    <div class="floating-shapes">
        <div class="shape"></div>
        <div class="shape"></div>
        <div class="shape"></div>
    </div>

    <div class="payment-container">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-8 col-xl-6">
                    <div class="card payment-card fade-in">
                        <div class="payment-header">
                            <button class="back-btn" onclick="goBack()">
                                <i class="fas fa-arrow-left"></i>
                            </button>
                            <h1><i class="fas fa-crown me-2"></i>Upgrade to Premium</h1>
                            <p>Unlock all premium features and take control of your finances</p>
                        </div>
                        
                        <div class="card-body p-4">
                            <!-- Premium Status Display -->
                            <div class="text-center mb-4">
                                <% if (user.ispremimumuser) { %>
                                    <div class="premium-badge">
                                        <i class="fas fa-crown me-1"></i>🌟 You are already a Premium User!
                                    </div>
                                <% } else { %>
                                    <div class="premium-badge">
                                        <i class="fas fa-user me-1"></i>👤 Regular User - Upgrade Now!
                                    </div>
                                <% } %>
                            </div>

                            <!-- Success/Error Messages -->
                            <div id="alertContainer"></div>

                            <% if (user.ispremimumuser) { %>
                                <!-- Already Premium User -->
                                <div class="alert alert-success">
                                    <h5><i class="fas fa-check-circle me-2"></i>Congratulations!</h5>
                                    <p class="mb-0">You are already a premium user and have access to all premium features!</p>
                                </div>
                                
                                <div class="text-center mt-4">
                                    <a href="/dashboard" class="btn btn-success btn-lg">
                                        <i class="fas fa-tachometer-alt me-2"></i>Go to Dashboard
                                    </a>
                                </div>
                            <% } else { %>
                                <!-- Premium Features List -->
                                <div class="row mb-4">
                                    <div class="col-md-6">
                                        <h5 class="mb-3"><i class="fas fa-star me-2"></i>Premium Features</h5>
                                        <ul class="features-list">
                                            <li><i class="fas fa-check"></i>Income Tracking</li>
                                            <li><i class="fas fa-check"></i>Advanced Reports</li>
                                            <li><i class="fas fa-check"></i>Leaderboard Access</li>
                                            <li><i class="fas fa-check"></i>Priority Support</li>
                                            <li><i class="fas fa-check"></i>Export Data</li>
                                            <li><i class="fas fa-check"></i>Custom Categories</li>
                                        </ul>
                                    </div>
                                    <div class="col-md-6">
                                        <h5 class="mb-3"><i class="fas fa-shield-alt me-2"></i>Benefits</h5>
                                        <ul class="features-list">
                                            <li><i class="fas fa-check"></i>Unlimited Transactions</li>
                                            <li><i class="fas fa-check"></i>Data Backup</li>
                                            <li><i class="fas fa-check"></i>Multi-Device Sync</li>
                                            <li><i class="fas fa-check"></i>24/7 Support</li>
                                            <li><i class="fas fa-check"></i>No Ads</li>
                                            <li><i class="fas fa-check"></i>Future Updates</li>
                                        </ul>
                                    </div>
                                </div>

                                <!-- Price Display -->
                                <div class="price-display">
                                    <div class="price-amount">₹299</div>
                                    <div class="price-period">per month</div>
                                </div>

                                <!-- Payment Form -->
                                <form id="paymentForm">
                                    <div class="form-floating position-relative">
                                        <input type="text" class="form-control" id="customerPhone" name="customerPhone" placeholder="Enter your phone number" required>
                                        <label for="customerPhone">Phone Number</label>
                                        <i class="fas fa-phone input-icon"></i>
                                    </div>
                                    
                                    <div class="form-floating position-relative">
                                        <input type="email" class="form-control" id="customerEmail" name="customerEmail" placeholder="Enter your email" value="<%= user.email %>" required>
                                        <label for="customerEmail">Email Address</label>
                                        <i class="fas fa-envelope input-icon"></i>
                                    </div>

                                    <div class="alert alert-info">
                                        <h6><i class="fas fa-info-circle me-2"></i>Payment Details</h6>
                                        <ul class="mb-0">
                                            <li>Amount: ₹299 (Fixed)</li>
                                            <li>Customer ID: Your User ID (Auto-filled)</li>
                                            <li>Secure payment powered by Cashfree</li>
                                        </ul>
                                    </div>

                                    <button type="submit" class="btn btn-payment" id="payButton">
                                        <i class="fas fa-credit-card me-2"></i>Pay Now - ₹299
                                    </button>
                                    
                                    <div class="text-center mt-3">
                                        <small class="text-muted">
                                            <i class="fas fa-shield-alt me-1"></i>
                                            Secure payment powered by Cashfree
                                        </small>
                                    </div>
                                </form>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Global variables
        let authToken = '<%= user.token || "" %>';
        let userId = <%= user.id %>;
        let isPremium = <%= user.ispremimumuser %>;
        let cashfree = null;

        // Handle SDK loading error
        function handleSDKError() {
            console.error('Failed to load Cashfree SDK from CDN');
            showAlert('Payment SDK failed to load. Please check your internet connection.', 'warning');
        }

        // Initialize Cashfree SDK
        async function initializeCashfree() {
            try {
                // Check if Cashfree is available
                if (typeof window.Cashfree === 'undefined') {
                    throw new Error('Cashfree SDK not loaded');
                }

                cashfree = await window.Cashfree({
                    mode: "sandbox" // Change to "production" for live payments
                });
                console.log('Cashfree SDK initialized successfully');
                showAlert('Payment system ready!', 'success');
            } catch (error) {
                console.error('Cashfree SDK initialization error:', error);
                showAlert('Payment SDK initialization failed. Using direct redirect method.', 'warning');
                // Don't block the payment flow - we can still redirect to Cashfree
            }
        }

        // Show alert messages
        function showAlert(message, type = 'info') {
            const alertContainer = document.getElementById('alertContainer');
            const alertClass = `alert-${type}`;
            const iconClass = type === 'success' ? 'fa-check-circle' : 
                             type === 'danger' ? 'fa-exclamation-triangle' : 'fa-info-circle';
            
            alertContainer.innerHTML = `
                <div class="alert ${alertClass} alert-dismissible fade show" role="alert">
                    <i class="fas ${iconClass} me-2"></i>${message}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            `;
        }

        // Go back to dashboard
        function goBack() {
            window.location.href = '/dashboard';
        }

        // Handle payment form submission
        document.getElementById('paymentForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            if (isPremium) {
                showAlert('You are already a premium user!', 'success');
                return;
            }

            const formData = new FormData(this);
            const customerPhone = formData.get('customerPhone');
            const customerEmail = formData.get('customerEmail');

            // Validation
            if (!customerPhone) {
                showAlert('Please enter your phone number', 'danger');
                return;
            }

            if (!customerEmail) {
                showAlert('Please enter your email address', 'danger');
                return;
            }

            // Start payment process
            await processPayment(customerPhone, customerEmail);
        });

        // Process payment
        async function processPayment(customerPhone, customerEmail) {
            const payButton = document.getElementById('payButton');
            const originalText = payButton.innerHTML;
            
            try {
                // Show loading state
                payButton.innerHTML = '<div class="loading-spinner me-2"></div>Processing...';
                payButton.disabled = true;
                showAlert('Creating payment order...', 'info');

                // Create payment order
                const response = await fetch('/payment-api/create-order', {
                    method: 'POST',
                    headers: {
                        'Authorization': `Bearer ${authToken}`,
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({
                        amount: 299,
                        customerId: userId,
                        customerPhone: customerPhone,
                        customerEmail: customerEmail,
                        returnUrl: window.location.origin + '/payment-success'
                    })
                });

                const result = await response.json();

                if (result.success) {
                    showAlert('Payment order created successfully. Redirecting to payment gateway...', 'success');
                    
                    // Redirect to Cashfree payment gateway
                    setTimeout(() => {
                        const paymentUrl = `https://sandbox.cashfree.com/pg/web/pay/${result.data.paymentSessionId}`;
                        console.log('Redirecting to:', paymentUrl);
                        window.location.href = paymentUrl;
                    }, 2000);
                } else {
                    showAlert(result.message || 'Error creating payment order', 'danger');
                }
            } catch (error) {
                console.error('Payment error:', error);
                showAlert('Payment process failed. Please try again.', 'danger');
            } finally {
                // Reset button state
                payButton.innerHTML = originalText;
                payButton.disabled = false;
            }
        }

        // Initialize on page load
        document.addEventListener('DOMContentLoaded', function() {
            if (!isPremium) {
                // Try to initialize Cashfree SDK, but don't block if it fails
                setTimeout(() => {
                    initializeCashfree();
                }, 1000);
            }
            
            // Add smooth animations
            const cards = document.querySelectorAll('.payment-card');
            cards.forEach((card, index) => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                setTimeout(() => {
                    card.style.transition = 'all 0.5s ease';
                    card.style.opacity = '1';
                    card.style.transform = 'translateY(0)';
                }, index * 100);
            });
        });

        // Phone number formatting
        document.getElementById('customerPhone').addEventListener('input', function() {
            let value = this.value.replace(/\D/g, '');
            if (value.length > 10) {
                value = value.substring(0, 10);
            }
            this.value = value;
        });
    </script>
</body>
</html>
